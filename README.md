# demo
1. git status
2. git checkout -b NameTheBranch    # Create a new branch  
3. git add . or git add ReadmeFile  # get add the file
4. git commit -m "reason"           # file to be stage
5. git push
6. git push --set-upstream origin new_branch # For the new branch
7. git pull